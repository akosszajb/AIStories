import React from "react";
import "./PageTitle.css";
import "../../../globals.css";

const PageTitle = ({ title }) => {
  return <h2 className="welcometitle">{title}</h2>;
};

export default PageTitle;
